#include <iostream>
#include <map>
using namespace std;

int main(){
    int n;
    cin>>n;
    vector<int> v;
    for(int i=0; i<n; i++){
        int m;
        cin>>m;
        v.push_back(m);
    }
    int k;
    cin>>k;
    int d=k%n;
    vector<int> nv;
    for(int i=d; i<n; i++){
        nv.push_back(v[i]);
    }
    for(int i=0; i<d; i++){
        nv.push_back(v[i]);
    }
    
    for(int i=0; i<n; i++){
        cout<<nv[i]<<" ";
    }
 
}

(* #approach 2 using recurssion  *)

#include <iostream>
#include <map>
using namespace std;
void dshift(vector<int> &v , int n, int d){
    if(d==0) return;
       int k=v[0];
       for(int i=0; i<n-1; i++){
        v[i]=v[i+1];
       }
       v[n-1]=k;
       dshift(v , n , d-1);
}
int main(){
    int n;
    cin>>n;
    vector<int> v;
    for(int i=0; i<n; i++){
        int m;
        cin>>m;
        v.push_back(m);
    }
    int d;
    cin>>d;
    dshift(v, n, d);
    for(int i=0; i<n; i++){
        cout<<v[i]<<" ";
    }
 
}
